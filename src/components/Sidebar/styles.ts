import styled from "styled-components";

export const SidebarContainer = styled.aside`
  width: 20%;
  height: 100%;
  padding-left: 1.5rem;
  margin-top: 1.5rem;
  p {
    font-weight: bold;
    color: var(--gray400);
    font-size: var(--small);
    margin-bottom: 1.5rem;
  }
`;